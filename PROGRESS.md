# PROGRESS

Branch: `feat/portal-and-elevation` (not pushed)
Plan: /Users/amol/.claude2/plans/lovely-gathering-hinton.md

## Done (2026-07-17)
- P1 cleanup (2a42e65): deleted styles.css, index_elite.html, uploads/, dead script.js blocks (year/eml/logVisit), copy_index footer link; ARIA tablist/tab/tabpanel added.
- CV refresh: assets/docs/CV_Amol_Thakre.pdf = Scientist_Educator version, 358,809 bytes (byte-verified vs ~/Desktop/CV_Final_2026/05_Academic_Educator/).
- P2 portal: portal-login.html, portal.html, portal.js, portal.css, portal/setup.sql + teaching-tab entry banner. Supabase URL/anon key still placeholders (portal.js:7-8).
- P3 visuals: hero3d.js (Three.js CDN, gated: reduced-motion/pointer:fine/width≥900/WebGL, silent 2D fallback), bento 3D tilt, magnetic CTAs, tab crossfade, gradient-ink headline, gold stat numerals. 2D flow-field now pauses on teaching tab (bug fix).
- P4 Opus gate: PASS-WITH-FIXES, zero blockers. Applied a11y nit (tab ids + aria-labelledby). Skipped: hidden-panel toggle (crossfade interaction), --danger token (matches existing convention), RLS inactive-common (by design).
- P5: portal/SETUP.md handoff doc.

## Ledger
- FACT: hosting = GitHub Pages static (amolthakrein-glitch.github.io/research_teaching_webpage), remote origin github.com/amolthakrein-glitch/research_teaching_webpage.git — no backend possible; auth = Supabase client-side + RLS.
- FACT: registration_server.py = local-only dev tool; its /admin has NO auth — never expose.
- FACT: index.html canonical; index_elite.css is the live stylesheet; support.js/Portfolio.dc.html = separate AI-builder artifact, untouched.
- DECISION: material gating by class+track enum: foundation-10, class11-neet, class11-jee, class12-neet, class12-jee (+ common/) — mirrors Course Pathways.
- DECISION: signups disabled in Supabase; enrollment = dashboard invite + students-row insert (SETUP.md §6).

## Round 2 (2026-07-17, later)
- Fable review fixes (prev commit): popup-safe downloads ({download:true} + anchor click), skip login when session exists.
- 8714f44 (Sonnet, Opus gate PASS): OPEN_ACCESS=true interim mode (portal.js:10 — flip false + drop "interim open access read" anon policy to restore auth); portrait fix (.portrait-ring max 340px, aspect-ratio 1/1 cover); CSS-only solar system in hero (hidden <900px + reduced-motion).
- Gate NITs accepted: hero overflow:hidden clips portrait hover-glow at edges; login→portal redundant hop when unconfigured.

## Round 3 (2026-07-18) — publish pipeline Upload→materials
Branch: main. Uncommitted: publish_materials.py (new), portal.js (+listTracks, trackLabel prettify fallback), portal.html (dropdown from manifest keys). Untracked junk not mine: .thumbnail, APP_SPEC.md, graphify-out/, support.js.
- FACT: publish_materials.py — sync ~/home/Academics_Education/resources/Upload → materials/<track>/ (root files→common), rebuild manifest from disk, commit `chore: publish materials`, push, ls-remote verify. --dry-run supported.
- VERIFIED: `python3 -m py_compile publish_materials.py` OK; `node --check portal.js` OK; `python3 publish_materials.py --dry-run` with 2 test PDFs → "synced: 2 new, 0 updated" exact paths correct.
- E2E DONE: b132511 (feat, code) → ee82ea1 (publish 2 test PDFs) → live verified (curl https://amolthakrein-glitch.github.io/.../materials/class11-math/test_notes.pdf = 200, 193 B exact) → fd839b0 (cleanup, test files removed everywhere, Upload empty). Rerun → "nothing to publish". All pushed, ls-remote verified.
- FACT: manifest track keys persist once created even when dir emptied (union with existing keys) — to retire a track, delete its key from manifest.json by hand + rm dir.
- Workflow now: drop file in Upload/<track>/ (or root → common) → `python3 publish_materials.py` → live in ~1 min.
NEXT: none — pipeline complete.

## Pending
- User: Supabase project setup per portal/SETUP.md (steps 1-6), then fill portal.js:7-8.
- E2E portal verification (SETUP.md §7) once project exists.
- Push/PR only on explicit "ship".

## Round 4 (2026-07-27) — design pass: color rebalance, hero motif, grid asymmetry
Branch: main. Uncommitted: index.html, index_elite.css, script.js (not yet committed).
- DECISION (user-confirmed via AskUserQuestion): cyan = sole dominant accent site-wide; gold restricted to 3 spots only (Two-Year "Enroll Now" CTA, NEET published book, Industry-Experience bento card). Solar-system hero motif (interactive planets, ss-* CSS, script.js click handlers) fully removed — replaced with ambient flow-field SVG (4 animated dashed streamline paths, .flow-field class) tying to fluid-dynamics bio. Bento/course/book grids get margin-top stagger (min-width:901px only) to break row-aligned symmetry. Hero h1 shimmer keyframe dropped (kept static gradient) — was double-animating with heroReveal.
- Testimonials/social-proof section explicitly skipped (no real data yet, user declined placeholders).
- VERIFIED: `node --check script.js` OK, CSS/JS brace balance OK, grep confirms zero leftover ss-*/solar-system/planet-card refs across html/css/js.
- False alarm resolved: mobile full-page (`--full-page`) screenshot artifact from Playwright's scroll-stitching desyncing the `animation-timeline: view()` reveal — not a real bug. Fixed-tall-viewport capture confirmed mobile renders correctly.
- REAL BUG FOUND + FIXED (pre-existing, predates this session): `.courses-grid` and `.pillars-grid` never had `display: grid` set anywhere in index_elite.css — they were laying out as plain stacked `<div>`s (block, full-width) instead of 3-column grids, on ALL viewport widths including desktop. Also fixed a `minmax(min(100%, 280px), 1fr)` pattern (should just be `minmax(280px, 1fr)`) that was contributing to the collapse. Added `display: grid;` to both selectors (index_elite.css:581-588). Verified via computed-style + rendered width: course-card went from 1300px (1-col) to 420px (3-col) at 1440 viewport; mobile still correctly stacks to 366px (1-col) at 390 viewport.
- VERIFIED (Playwright/Chromium screenshots, both tabs, desktop 1440 + mobile 390): hero flow-field renders (4 animated dashed streamlines, cyan+sparse-gold), cyan-dominant palette confirmed throughout, gold confirmed scarce (Enroll Now CTA x2, NEET book, Industry-Experience card only), bento-grid stagger visible, courses-grid now real 3-column with featured card lift, books-grid 2-up with offset.

## Round 5 (2026-07-27, later) — site-wide brand unification (in progress)
Branch: main. Uncommitted (this round, on top of Round 4): registration.html (full re-skin), index_elite.css (+.detail-hero/.detail-card/.logo-overlay shared classes, +.art-math/.art-ai new SVG patterns), edu_detail.html (reference re-skin, Unsplash removed), summer_school.html (agent re-skin, done), campaign.html (agent re-skin, done).
- FINDING: site ran 4 unrelated visual languages depending which link you click (dark elite index_elite.css vs campaign.css light theme vs registration.html's own inline Manrope/blue-red theme vs summer_school.css bespoke theme) — worst at the exact conversion moment (Enroll Now → campaign → registration all looked unrelated). User confirmed (AskUserQuestion): full re-skin of all 3 onto elite dark system + replace Unsplash stock photos with abstract SVG art site-wide.
- DECISION: registration.html — did directly (Opus, conversion-critical + custom JS). Removed dead `fetch('/api/register')` call (no backend exists, GitHub Pages static-only per Round 3 FACT) and the misleading "obfuscation" comment (contact info is meant to be public). All WhatsApp/call/email link-generation JS preserved exactly. VERIFIED: node --check pass, div-balance match, 0 `/api/register` refs, Playwright render clean, wa/call hrefs correct, 0 console errors.
- DECISION: campaign.html, summer_school.html, and 5 remaining detail pages (exp/phil/tech/lab/impact_detail.html) delegated to 3 parallel Haiku agents with prescriptive specs (exact class names, exact art-* mappings, "preserve all hrefs/copy/scripts exactly").
- campaign.html agent: DONE. Self-reported: 0 unsplash refs, 0 campaign.css refs, node --check pass. Art mapping: Digital Twin→art-lab, Concept-to-Competition→art-phil, Analytics→art-tech, Peer Group→art-industry, Industrial Logic→art-industry, Seats-Left→bento-gold stat card (no art). NOT YET independently re-verified by me (only agent's self-report so far).
- summer_school.html agent: DONE + independently verified by me: 0 unsplash, 0 summer_school.css refs, elite-body/elite-container present, div-balance 28/28 match.
- 5-detail-pages agent (exp/phil/tech/lab/impact_detail.html): STILL RUNNING as of this checkpoint. Partial grep snapshot mid-run: exp_detail=0, phil_detail=0 unsplash already; tech_detail=6, lab_detail=4, impact_detail=4 unsplash still present (not yet done touching those 3).
- index_elite.css new shared classes added: .detail-hero/.detail-hero h1/.detail-section/.detail-card/.detail-card h3/.detail-card p/.detail-card-media img/.logo-overlay (dedupes what was a copy-pasted inline <style> block per detail page), .art-math (vedic-math number-grid motif), .art-ai (neural-network node motif) — both new art classes added after .art-impact.
- VERIFIED SO FAR (this round): node --check script.js PASS; index_elite.css brace balance 259/259 MATCH; div-balance MATCH on registration.html(5/5)/summer_school.html(28/28)/edu_detail.html(11/11); 0 `/api/register` refs; 0 unsplash refs in registration/summer_school/edu_detail.

- All 3 agents completed. Independently re-verified (did NOT trust self-reports): grep unsplash=0 across all 9 touched files, div-balance MATCH on all, campaign.css ref=0.
- BUG FOUND + FIXED (introduced by exp_detail.html agent): its 3 photo+text cards used inline `style="grid-template-columns: 1.2fr 1fr;"` but dropped `display:grid` (copy-paste miss from the edu_detail.html reference pattern) — rendered as block, logos escaped their box at native pixel size since the agent also invented an undefined `.logo-box` class with no img size constraint. Fixed: added `display:grid` back to the 3 inline styles (exp_detail.html), added `.logo-box`/`.logo-box img` component to index_elite.css (60x60 default, object-fit:contain, overflow:hidden).
- BUG FOUND + FIXED (pre-existing in the design, not agent-caused): detail-card 2-column layouts (edu_detail + exp_detail) had no mobile breakpoint — inline grid-template-columns doesn't collapse to 1-col on small screens, and absolutely-positioned .logo-box badges then overlapped stacked text. Fixed with two mobile-only (`max-width:640px`) rules in index_elite.css: `.detail-card[style*="grid-template-columns"] { grid-template-columns:1fr !important }` and `.detail-card .logo-box { position:static !important; margin:16px 0 !important }`. Verified via Playwright screenshot at 390px — clean, no overlap.
- NOTED, not fixed (out of scope, no replacement asset): lab_detail.html's two `<video>` tags point at assets/videos/*.mp4 which don't exist (dir deleted in an earlier "remove orphaned files" commit, video tags never updated) — pre-existing, confirmed via git diff my agent didn't touch video src. Renders as empty black player, not a regression from this round.
- FINAL VERIFICATION (all real, just run): 0 `images.unsplash.com` refs across all 9 touched files (campaign/summer_school/registration/edu/exp/phil/tech/lab/impact_detail.html); index_elite.css brace balance 263/263 MATCH; `node --check script.js` PASS; Playwright screenshots taken + reviewed for every re-skinned page at desktop widths, plus mobile for exp_detail/edu_detail (the 2 with the grid-collapse fix) — all render correctly, no console errors, no overlap.

Round 5 was committed+pushed (54bafd4, verified on remote via ls-remote).

## Round 6 (2026-07-27, later still) — adversarial audit: SEO/discovery/hygiene gaps
Branch: main. Uncommitted this round: favicon.svg (new), robots.txt (new), sitemap.xml (new), archive/campaign.css + archive/summer_school.css (moved via git mv), index.html, script.js, + favicon <link> tag added to all 13 other .html files (campaign/edu/exp/impact/join-now/lab/phil/portal-login/portal/registration/summer_school/tech_detail/whatsapp_flyer_2026).
- FINDING: zero favicon anywhere, zero JSON-LD structured data, no robots.txt/sitemap.xml, teaching tab not deep-linkable (JS-only display:none toggle, no hash routing), summer_school.html fully orphaned (nothing on the site links to it — verified via grep), campaign.css/summer_school.css dead since Round 5 re-skin. User confirmed (AskUserQuestion) all 3 recommended fixes.
- DECISION: favicon.svg = simple cyan "AT" monogram on dark rounded-square bg (no existing logo asset to derive from). Added to index.html directly, then delegated the mechanical rollout to the other 13 files to a Haiku agent (verified after: grep -L across all — only Portfolio.dc.html correctly excluded, out of scope).
- DECISION: JSON-LD Person schema added to index.html only (the canonical profile page) — name/alumniOf (Twente+IISc, both verified facts)/knowsAbout, no sameAs (no social profile links exist anywhere on the site — did not fabricate one).
- DECISION: hash-based deep-linking added to script.js's tab logic (extracted inline click handler into a `switchTab(key, {animate, updateHash})` function; uses `history.replaceState` not `pushState` so tab-flipping doesn't spam back-button history; reads `location.hash` on load to open the right tab pre-animation). VERIFIED via Playwright: loading `index.html#teaching` opens the teaching tab directly; clicking the tab updates the hash to `#teaching`.
- DECISION: added a "Summer School 2026" discovery card (matching the existing `.portal-banner-card` pattern) to the teaching tab, right below the student-portal-login banner, linking to summer_school.html.
- campaign.css/summer_school.css moved to new `archive/` dir via `git mv` (preserves history, reversible) — confirmed zero remaining references in any .html file before moving.
- VERIFIED (all real, just run): `node --check script.js` PASS; div-balance index.html 70/70 MATCH; favicon renders correctly as a crisp cyan "AT" on dark rounded square (Playwright screenshot of favicon.svg alone); `grep -L favicon.svg *.html` returns only Portfolio.dc.html (correctly out of scope); Playwright screenshot of `index.html#teaching` confirms tab opens directly + Summer School card renders correctly.

NEXT: Round 6 complete and verified, not yet committed. Ask user before committing/pushing — scope to: favicon.svg, robots.txt, sitemap.xml, archive/campaign.css, archive/summer_school.css (as renames), index.html, script.js, campaign.html, edu_detail.html, exp_detail.html, impact_detail.html, join-now.html, lab_detail.html, phil_detail.html, portal-login.html, portal.html, registration.html, summer_school.html, tech_detail.html, whatsapp_flyer_2026.html, PROGRESS.md. Do NOT stage .serena/project.yml (unrelated tool auto-config) or any of the untracked junk (.thumbnail, APP_SPEC.md, graphify-out/, support.js).

## Round 7 (2026-07-28) — hero flow motif replaced with real Joukowski airfoil potential-flow sim
Branch: main. Plan: /Users/amol/.claude/plans/use-opus-to-plan-wild-eclipse.md (Opus-authored, approved). Uncommitted this round: hero-airfoil.js (new), index.html (§1 edit), index_elite.css (§2 edit).
- DECISION: replaced the 4-path decorative SVG dashed-streamline motif (from Round 4) with an analytic 2D potential-flow solution around a Joukowski airfoil (conformal map, closed-form Kutta-condition circulation) — physically real, not a fake animation, appropriate for a PhD fluid-dynamicist's hero. Rendered via `<canvas>` + rAF particle advection (RK2), static RK4 streamlines under `prefers-reduced-motion`. Same `.flow-field` box/geometry preserved exactly; `script.js` untouched (tab/scroll/visibility pause wired via IntersectionObserver+MutationObserver+visibilitychange from the new file only).
- Execution: Opus planned (full math spec + JS function list + params + risk register written out so executor didn't have to derive anything) → Haiku built the 3 files per plan.
- BUGS FOUND + FIXED (by me, post-Haiku, before reporting done): (1) colorLUT alpha channel pushed as 0-255 int into `rgba()` — CSS clamps that to fully opaque, would've killed the entire speed→brightness visual read; fixed to 0-1 float. (2) `lineWidth`/`shadowBlur` multiplied by `dpr` a second time on top of the already-dpr-scaled canvas transform — lines/borders would render ~2x too thick on retina; removed the redundant multiply in 5 places. (3) `DEN_FLOOR` (1e-4) guard computed but unused, `cdiv`'s own 1e-12 floor was silently used instead at the TE singularity; replaced with a manual divide using the mandated floor, matching the plan's math spec exactly.
- VERIFIED (real, just run): `node --check hero-airfoil.js` PASS. CSS brace balance 256/256 MATCH. `python3 html.parser` full-file parse: 0 errors. `#hero-airfoil-canvas` id + `hero-airfoil.js` script tag each appear exactly once in index.html. `script.js` confirmed untouched by this task (content matches pre-task read; its unrelated diff vs HEAD predates this session — same for ~15 other pre-existing dirty files, not touched here).
- VERIFIED (physics, via standalone Node re-implementation of the fixed formulas, not the browser): Kutta condition — TE speed converges smoothly to ~0.91U as θ→0°, no blow-up, VMAX clamp never triggers. Stagnation point — sits below geometric LE (pressure side) and migrates further aft/down as α goes 2°→8° (θ 188°→200°, y −0.024→−0.085) — matches expected physics exactly. Far-field — converges toward U(cosα,0) with residual Vy≈U·sinα (that residual IS the upwash signature, expected at finite ~5-chord domain).
- NOT YET DONE (needs a real browser, could not run here): plan §9.2 checks — CLS, frame budget/60fps, pause-on-tab-switch/scroll-away/hidden-tab, `prefers-reduced-motion` static-mode render, mobile emulation, console-error check. User asked to open `python3 -m http.server 8080` in that dir and eyeball plan §9.1 (six physics criteria by eye) + §9.2.

- FOLLOW-UP FIX (user tested locally, flagged color scheme changing mid-sim): the speed-color ramp had a 4th tier ramping bright-cyan → **gold** at the suction peak. This violates the site's own Round 4 DECISION (cyan sole dominant accent, gold restricted to exactly 3 CTA spots) — the hero motif was quietly adding a 4th, animated gold usage. Fixed: dropped the gold tier entirely, ramp now caps at bright-cyan (`buildColorLUT` in hero-airfoil.js). Removed now-dead `palette.gold` field + its `--gold` read in `init()`.
- VERIFIED (real, just run, post-fix): `node --check hero-airfoil.js` PASS; `grep -c "palette.gold\|--gold" hero-airfoil.js` = 0; CSS brace balance 256/256 MATCH; `script.js` still untouched (switchTab present, 3 refs, unrelated pre-existing diff only).
- LESSON recorded: `/Users/amol/home/Career_Professional/Profiles_and_Webpage/MISTAKES.md` (new file) — new decorative color additions must be checked against existing brand-color DECISIONs in PROGRESS.md before building, not after.

- FOLLOW-UP (user: "smaller window, faster flow, lighter streamlines, show wake formation"):
  - Domain narrowed 10→7.8 width (`X_MIN -3.3/X_MAX 4.5`, shifted downstream for wake room) — chord now ~52% of frame vs ~40%, visibly zoomed. `SPEED_SCALE` 2.5→3.6, particle lifetime 9-15s→5.5-9.5s to match.
  - REAL BUG FOUND + FIXED: `readColor()` parsed hex tokens (`#22d3ee`) with a decimal-digit regex meant for `rgb()` strings — silently produced `[22,3,NaN]`, an invalid color the browser was rejecting. Replaced with correct hex-triplet parsing.
  - Lighter streamlines: base/peak colors now blend `--slate`/`--cyan` toward `--text` (token-derived, no new literals); alpha floor 0.20→0.34, ceiling 0.78→0.88.
  - REAL BUG FOUND + FIXED in `drawWake`: `atan2(-1,dx)` then `tan()*dx` mathematically collapsed to a constant −1 unit offset for any dx>0 — a hard jump right at the trailing edge, not a droop (explains why the wake wasn't visible/looked broken). Replaced with an alpha-scaled downwash slope (`tan(-1.6·α)`, verified in Node: droop grows smoothly 0.14→0.57 units as α 2°→8°) + animated shed-vortex wobble, cyan-tinted, more visible (alpha 0.18).
  - VERIFIED: `node --check` PASS; wake math sanity-checked in Node across full downstream range (0 NaN/Infinity, monotonic growth with α).

- FOLLOW-UP (user: "way off currently, more realistic fluid-like flow"): **root-cause bug found and fixed** — `stepParticles` never updated each particle's "previous position" (ppx/ppy), which stayed frozen at the seed point forever. Every frame was drawing an ever-lengthening straight rubber-band from the original seed point to the current position, not a short streak — this was almost certainly the entire "way off" complaint, not a matter of tuning. Fixed: pre-step position now recorded each frame, so segments chain continuously frame-to-frame (verified via a standalone Node continuity smoke test: each frame's start == last frame's end). Also lengthened trail persistence (`FADE` 0.14→0.09 desktop, 0.20→0.14 mobile) for smoother streaklines now that they compose correctly.
  - Color-by-velocity-and-pressure: no new code needed — the existing speed ramp already *is* the pressure field (`Cp = 1 − (v/U)²` is a direct function of speed under Bernoulli); documented this reasoning back to the user rather than adding a redundant separate Cp computation.
  - VERIFIED (real, just run): `node --check hero-airfoil.js` PASS; gold refs = 0; CSS brace balance 256/256 MATCH; html.parser full parse 0 errors; `script.js` untouched (3 switchTab refs); trail-continuity logic smoke-tested in Node (confirmed unbroken chaining).

- FOLLOW-UP (user: "graphics behind the airfoil, not sure what that is" — investigated with actual screenshots this time, not guesswork): installed Playwright (`/opt/homebrew/bin/python3.14 -m pip`/`playwright install chromium` — the repo's default `python3` resolves to a brain/.venv without playwright, homebrew python3.14 has it) and screenshotted the live canvas. Found and fixed two real bugs:
  - **Launch-transient bug**: `initParticles()` seeded every particle at the inlet simultaneously, so for the first several seconds after page load they travelled together as a synchronized wavefront — this rendered as a solid "wall/curtain" (the mystery graphics). Confirmed by comparing a 1.5s screenshot (solid wall) vs a 10s screenshot (clean flow) of identical code. Fixed: particles now scatter across the whole domain with randomized starting age at init.
  - **Density bug**: independently, trail persistence × speed × particle count was covering ~50% of the frame in ink at any instant, which would smear into a solid mass regardless of the transient. Cut particle count, shortened trail persistence (`FADE` up), and — separately — culled rendering of near-stagnant particles (`q < DRAW_MIN_Q`) plus sped up their recycle-eviction (`SLOW_SPEED_Q`/`SLOW_AGE_PENALTY`), since slow particles were redrawing the same spot for many frames and saturating to opaque. Domain widened slightly (7.8→8.6 width) so the stagnation-point streamline convergence has visual room to fan out.
  - VERIFIED: screenshot sequence at 0.5s/1.5s/3s/5s/10s all clean, no wall at any point. `node --check` PASS.
- FOLLOW-UP (user: "inject more particles, different colors for velocities, remove tail behind airfoil"):
  - Particle count 520→850 desktop, 260→420 mobile (kept trail length constant so density stayed safe).
  - Real multi-hue velocity ramp added: slate (near-stagnant) → **blue** `#3b82f6` (decelerated/pressure-side) → cyan (near free-stream) → bright cyan-white (suction peak). Blue is this project's own documented accent color per `CLAUDE.md` (not previously wired as a CSS token here) — not a new invented literal, and not gold, so it doesn't reopen the earlier gold-consistency issue.
  - Removed `drawWake()` entirely (function + both call sites) — no more wake/tail structure behind the trailing edge.
  - VERIFIED: zoomed screenshot on the leading edge showed clean stagnation split + visible blue tint on slow segments vs cyan elsewhere. `node --check` PASS, 0 `drawWake`/gold refs.
- FOLLOW-UP (user: "increase particle velocities 2-3x"): `SPEED_SCALE` 3.6→8.5 (~2.4x). Faster particles draw proportionally longer per-frame segments at the same trail persistence, which would have re-triggered the density-wall bug just fixed — compensated by raising `FADE` (0.26→0.40 desktop, 0.30→0.44 mobile) and trimming particle count back down (850→650 desktop, 420→320 mobile) to keep total ink coverage in the same safe range.
  - VERIFIED: steady-state screenshots (two consecutive frames, ~10s+ post-load) show longer/faster-reading streaks, no wall, no wake tail, stagnation split and suction-side convergence still crisp. `node --check` PASS, CSS braces 256/256, `script.js` untouched (3 switchTab refs).

## Round 8 (2026-07-28, later) — campaign.html CTA copy
Branch: main. Uncommitted this round: campaign.html.
- Changed the admissions CTA at campaign.html:36 from `Enroll for March / June Batch →` to `Rolling admissions through December →` (only occurrence in the repo, grep-confirmed).
- FALSE ALARM investigated: user reported old text still showing after the fix. Verified via direct `curl localhost:8080/campaign.html` (bypassing browser) — server was already serving the corrected file correctly. Root cause: stale browser tab/cache on the user's end, not the file. No code change needed for that report.
- Also changed campaign.html:53 (scarcity card subtitle) from `March Early Batch` → `Rolling Admissions`, consistent with the CTA copy — user confirmed after I flagged it as a 1-line out-of-scope note.
- NOTED, not fixed (flagged to user, no response yet): campaign.html's `<title>` + og:title + twitter:title (lines 7/17/22) still read "Elite Mentorship March 2026" — same stale-date issue, different scope (meta tags vs visible copy).
- VERIFIED (real, just run): html.parser full parse of campaign.html — 0 errors both times. `grep` confirms both new strings present, old strings (`March / June Batch`, `March Early Batch`) gone repo-wide. Only campaign.html touched.

- Also changed campaign.html:36 CTA to just "Rolling Admissions →" (dropped "through December", matching the line-53 scarcity-card wording exactly, per user request).
- SHIPPED: user said "push it" for Rounds 6+7+8 together. Asked to confirm scope first (index.html has Round 6 + Round 7 changes intermixed, couldn't cleanly split by file) — user chose "everything uncommitted". Staged everything except `.serena/project.yml` (unrelated tool auto-config) and untracked junk (`.thumbnail`, `APP_SPEC.md`, `graphify-out/`, `support.js`), per this file's own long-standing exclusion note. Committed `d8140f6` (23 files, 997 insertions/57 deletions), pushed to `origin/main`, verified via `git ls-remote origin main` — remote SHA matches local exactly.

NEXT: this is a GitHub Pages static site — live site updates automatically from the push, no separate deploy step. Still outstanding: plan §9.2 browser-only checks (CLS, fps, pause-on-tab-switch, reduced-motion static render, mobile emulation, console errors) on the hero sim — not run yet, low urgency now that it's shipped and user has visually approved it interactively all session. Also still flagged, not actioned: campaign.html's `<title>`/og/twitter meta tags (lines 7/17/22) still say "Elite Mentorship March 2026" — ask user if that should change too.
